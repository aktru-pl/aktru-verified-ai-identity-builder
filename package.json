{
  "name": "aktru-verified-ai-identity-builder",
  "private": false,
  "version": "1.0.1",
  "description": "Local-first evidence tracing and machine-readable company identity builder for websites and registry data.",
  "author": "AKTRU Sp. z o.o.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22 <27"
  },
  "scripts": {
    "dev": "vite",
    "preview": "npm run build && wrangler dev",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "jszip": "^3.10.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.45.1",
    "@types/node": "^22.15.18",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "jsdom": "^26.1.0",
    "typescript": "~5.8.3",
    "vitest": "^3.1.3",
    "wrangler": "^4.112.0"
  }
}
